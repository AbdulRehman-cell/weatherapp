services:
  - type: web
    name: weather-app
    env: docker
    dockerfilePath: Dockerfile
    autoDeploy: true
    healthCheckPath: /
    healthCheckInterval: 300
    healthCheckTimeout: 10
    healthCheckRetries: 3