# This is the .env file template for environment variables

# Render API key for deployment
RENDER_API_KEY=your_render_api_key_here # Required for deploying to Render

# Render username for Docker login
RENDER_USERNAME=your_render_username_here # Required for deploying to Render